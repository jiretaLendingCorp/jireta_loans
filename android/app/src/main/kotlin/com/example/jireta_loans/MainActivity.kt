package com.example.jireta_loans

import io.flutter.embedding.android.FlutterFragmentActivity

// FlutterFragmentActivity (hindi FlutterActivity) — kailangan ito ng
// `local_auth` para maipakita ang biometric / device credential prompt
// (fingerprint, Face ID, o device PIN/password fallback).
class MainActivity : FlutterFragmentActivity()
